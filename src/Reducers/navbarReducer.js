let initialState = {
    navbar: [
        {id: 1, name: 'books'},
        {id: 2, name: 'cars'},
        {id: 3, name: 'films'}
    ]
}

const navbarReducers = (state = initialState, action) => {
    return state
}

export default navbarReducers